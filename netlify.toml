# Sermona docs site (VitePress). Set VITEPRESS_SITE_URL in Netlify UI to your
# canonical URL (e.g. https://sermona.yourdomain.com) so Open Graph / canonical tags resolve.
[build]
  command = "npm run build"
  publish = "docs/.vitepress/dist"

[build.environment]
  NODE_VERSION = "20"
