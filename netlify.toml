# Sermona docs site (VitePress). Canonical URL for OG / Twitter cards / JSON-LD (no trailing slash).
[build]
  command = "npm run build"
  publish = "docs/.vitepress/dist"

[build.environment]
  NODE_VERSION = "20"
  VITEPRESS_SITE_URL = "https://sermona-design-system.netlify.app"
