---
layout: home

hero:
  name: Your project
  text: Documentation
  tagline: Default Sermona shell — dark tokens, VitePress, and CSS primitives.
  actions:
    - theme: brand
      text: Guide
      link: /guide
---

## Write in Markdown

Add pages next to this file and list them in `.vitepress/config.ts` under `themeConfig.sidebar`.

## Optional HTML patterns

Use Sermona pattern classes inside raw HTML for rich layout — for example `.sermona-card`, `.sermona-quote`, grids, and icon tiles from `@sermona/tokens/components.css`.
